import { ListSplitterDirective } from './list-splitter.directive';

describe('ListSplitterDirective', () => {
  it('should create an instance', () => {
    const directive = new ListSplitterDirective();
    expect(directive).toBeTruthy();
  });
});
